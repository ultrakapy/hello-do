# hello-do

Throwaway repo for Block 1 only: confirm the GitHub -> DO App Platform deploy
pipeline (Dockerfile-based) works end to end before building anything real.

Push, connect to App Platform, deploy, curl `/` and `/healthz`, then move on.
Not the project skeleton - that's a separate, fuller artifact.
